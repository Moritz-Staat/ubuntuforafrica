services:
  web:
    build: .
    ports:
      - "3000:3000"
    env_file: .env.local
    environment:
      - NODE_ENV=production
    # Kein Bind-Mount des Quellcodes: das Image enthält den fertigen Build.
    # Nach Codeänderungen: docker compose up -d --build
    restart: unless-stopped
